const ICONS_DIR = "/icons";

const IMAGE_DIR = "/images";

const ROUTES = {
    auth: {
        LOGIN: "/auth/login",
        SIGNUP: "/auth/signup",
        MAIN: "/auth",
    },
    home: {
        HOME: "/",
    },
};

const CACHE_KEYS = {
    LOG_OUT: "LOG_OUT",
};

export { CACHE_KEYS, ICONS_DIR, IMAGE_DIR, ROUTES };
